import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.{js,ts,jsx,tsx,scss}'],
  plugins: []
  /*  corePlugins: {
    preflight: false // Disable preflight if necessary for testing
  } */
}
export default config
